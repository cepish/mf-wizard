const FRONT_DIR_NAME = "apps";
const TEMPLATE_NAME = "PROJECT_NAME";

const SCOPE_NEXT = "next";
const SCOPE_FRONT = "front";
const SCOPE_SERVER = "server";
const SCOPES_LIST = [SCOPE_FRONT, SCOPE_SERVER, SCOPE_NEXT];
const example = "mf-init front blog";

module.exports = {
  FRONT_DIR_NAME,
  SCOPE_NEXT,
  SCOPE_FRONT,
  SCOPE_SERVER,
  SCOPES_LIST,
  TEMPLATE_NAME,
  example,
};
