{
  "buildCommand": "pnpm --workspace-root exec turbo --no-daemon --filter PROJECT_NAME build --env-mode loose",
  "installCommand": "node -v && pnpm --workspace-root --filter PROJECT_NAME... install --config.dedupe-peer-dependents=false",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}
